import 'package:flutter/material.dart';
import 'package:point_roulette/app.dart';

void main() {
  // Flutter 엔진 초기화 (플러그인 사용 전 필수)
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const PointRouletteApp());
}
