# === 앱 빌드 환경변수 (--dart-define으로 전달) ===

# 환경 (dev / prod)
# flutter build apk --dart-define=ENV=prod
ENV=dev

# 프로덕션 WebView URL (배포된 web-user 주소)
# flutter build apk --dart-define=ENV=prod --dart-define=WEB_URL=https://your-web-user.vercel.app
WEB_URL=https://your-web-user.vercel.app
